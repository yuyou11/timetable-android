# ============================================================
#  .gitignore —— 告诉 git「哪些文件不要进版本库」
# ============================================================
#
# 判断标准只有一条：**这个文件是「人写出来的」，还是「机器生成出来的」？**
# 人写出来的（.kt / .xml / .md）要进版本库，因为丢了就找不回来；
# 机器生成出来的（编译产物）不该进，因为只要有源码随时能再生成一遍。
#
# 把生成物提交进去有两个坏处：
#   1. 仓库体积暴涨（这个项目里 build/ 一个目录就有 61.5 MB，而源码只有 1 MB）
#   2. 每次编译都产生几百个文件的「改动」，真正的代码改动被淹没了
#
# 规矩：**build 目录不该进版本库**，这一条在几乎所有语言里都成立。
# 网页项目里对应的是 node_modules/、dist/ 这些。

# ------------------------------------------------------------
#  Gradle / Android 编译产物
# ------------------------------------------------------------
# 前导斜杠表示「只匹配仓库根目录下的」，不写的话会匹配到任意层级的同名目录
/build/
/app/build/
/.gradle/

# 编译日志
/build-logs/

# 万一有人手动把 APK 拷到别处，也别让它进来
*.apk
*.aab
*.ap_
*.dex

# 打包产物目录（里面装的是 APK，属于「发布物」不是「源码」）
# 想发布 APK，正确做法是用 GitHub Releases 附件，而不是塞进仓库
# —— 仓库里的二进制文件会**永久留在历史里**，删掉文件也删不掉历史，只会越来越大
/dist/

# ------------------------------------------------------------
#  🔴 签名密钥 —— 这两行是整个文件里最重要的
# ------------------------------------------------------------
# 泄露的后果不是「密码被人看到」，而是「任何人都能冒充你发新版 App」。
#
# Android 判断「这是不是同一个应用」只看签名密钥。别人拿到这把钥匙，
# 他做的 APK 就能直接覆盖安装到你手机上已有的那个 App 上，系统会当成
# 正常更新，用户数据全都保留 —— 等于应用的发布权易主。
#
# 而且密钥泄露**没有「改密码」这种补救**：唯一办法是换一把新钥匙重新签名，
# 代价是所有已安装的用户必须先卸载重装（数据会丢）。
#
# release.keystore  = 私钥本体
# keystore.properties = 私钥的密码，明文存储
release.keystore
keystore.properties
*.jks
*.keystore

# ------------------------------------------------------------
#  本机专属配置
# ------------------------------------------------------------
# local.properties 里存的是「这台电脑的 Android SDK 装在哪」，
# 隔壁电脑上路径不一样，提交上去只会给别人添乱。
# Android Studio 打开项目时会自动重新生成一份。
local.properties

# ------------------------------------------------------------
#  编辑器 / 操作系统
# ------------------------------------------------------------
.idea/
*.iml
.DS_Store
Thumbs.db
captures/
.externalNativeBuild/
.cxx/
